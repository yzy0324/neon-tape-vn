# neon-tape-vn

一个纯静态 Web 视觉小说原型项目。

## 运行方式

在仓库根目录执行：

```bash
python -m http.server 8000
```

随后在浏览器打开：

- <http://localhost:8000>

## 自检

在仓库根目录执行：

```bash
python scripts/selfcheck.py
```

该脚本会检查：关键字符串、场景数量、结局存在与存档键存在。
